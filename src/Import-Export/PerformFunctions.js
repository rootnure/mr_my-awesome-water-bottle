import { add, mult, sub, devide as dev } from "./MyFunctions";

const result = add(5, 2);
const remaining = sub(55, 20);
const product = mult(4, 10);
const devide = 32 / 8;
const output = dev(30, 6);

console.log(result, remaining, product, devide, output);